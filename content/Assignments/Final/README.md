# Invisible Matters starter repo

Open `index.html` with VS Code Live Server, or run:

```bash
python3 -m http.server 8000
```

Then replace placeholders in `index.html`.

Assets:
- `assets/images`
- `assets/video`
